using UnityEngine;
using System.Collections;

public class BackgroundScript : MonoBehaviour {

	public Player player1;
	public Player player2;

	//public AudioClip backgroundMusic;
//	public Straw straw;
//	public StrawHolder strawHolder;
//	public PlayerMessage playerMessage;

	// Use this for initialization
	void Start () {
		//player1 = gameObject.AddComponent ("Player") as Player1;
		//player2 = gameObject.AddComponent ("Player") as Player2;
 		/*player1.SendMessage ("setTeam", -1);
		player1.SendMessage("setPos",false);
		player2.SendMessage ("setTeam", 1);
		player2.SendMessage ("setPos", true);*/
		//straw = gameObject.AddComponent ("Straw") as Straw;
		//strawHolder = gameObject.AddComponent ("StrawHolder") as StrawHolder;
	//	playerMessage = gameObject.AddComponent("PlayerMessage") as PlayerMessage;


	}
	
	// Update is called once per frame
	void Update () {
		/*
		if (backgroundMusic) {
			AudioSource.PlayClipAtPoint(backgroundMusic, new Vector3(0,0,0));
		}


		if (!strawHolder)
			playerMessage.setVictory ();
		if (Input.GetKey ("r")) {
			Time.timeScale = 1;
			Application.LoadLevel (0);
		}*/
			
	}
}
